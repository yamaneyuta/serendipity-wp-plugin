# TODO: 削除
query echo($message: String!) {
	echo(message: $message)
}


query PostSetting($postID: Int!) {
	postSetting(postID: $postID) {
		sellingPrice {
			amountHex
			decimals
			symbol
		}
	}
}
