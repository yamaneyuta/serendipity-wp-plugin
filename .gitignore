/.devcontainer/development-internal/compose.network.yml

# publicディレクトリはリリースファイル(zip)に含めるが、Git管理は行わない
/public

# graphql-codegenの生成ファイル
/src/types/gql

.devcontainer/.intelephense-includes

# 翻訳関連ファイル
# .potファイルはコマンドで毎回生成されるためGit管理不要
# .poファイルは各言語への翻訳が記載されているためGit管理対象
# .moファイルは.poファイルをコンパイルしたバイナリファイルのためGit管理対象外
*.pot
*.mo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.bak

# Coverage directory used by tools like istanbul
coverage

# Compiled binary addons (https://nodejs.org/api/addons.html)
/build

# Dependency directories
node_modules/
/.phpunit
.phpunit.result.cache
/includes/vendor/
/includes/assets/constants.json

/includes/cache/
!/includes/cache/.gitkeep

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Output of `npm pack`
*.tgz

# Output of `wp-scripts plugin-zip`
*.zip

# dotenv environment variables file
.env

# `package.json`で`.gitignore`の内容をコピーし
# prettierの対象外ディレクトリを追加する処理を行っている
# 動的に生成されるため、Gitの管理対象外とする
.prettierignore

*storybook.log
/storybook-static
